import { test, expect } from '@mobilewright/test';

test('deliberately fails, to see the automatic failure screenshot', async ({ screen }) => {
  // TODO: assert on text that does NOT exist on the home screen, run this,
  // and look at the screenshot attached to the failure in the HTML report.
});
